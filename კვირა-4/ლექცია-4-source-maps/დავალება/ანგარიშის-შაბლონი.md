# ანგარიში: Source Maps და Production Debugging — Order Desk

**სტუდენტი:** ___________________
**თარიღი:** ___________________
**პროექტი:** `homework-source-maps/`

---

## 1. dev vs production

### 1.1 ბილდი

| | მნიშვნელობა |
|---|---|
| `dist/assets/*.js` ფაილის სახელი | |
| JS ფაილის ზომა | |
| gzip ზომა | |
| `.map` ფაილი შეიქმნა? | |
| `dist/` ჯამური ზომა | |
| ხაზების რაოდენობა JS ფაილში | |
| `calculateTotals` არის ფაილში? | |

**რატომ (არ) არის `calculateTotals` მინიფიცირებულ ფაილში:**

### 1.2 რა იცვლება

| | `npm run dev` | `npm run build` + `preview` |
|---|---|---|
| Bundling | | |
| Minification | | |
| Source maps | | |
| React-ის ბილდი | | |
| ფუნქციების სახელები | | |
| გამაფრთხილებლები | | |

---

## 2. ბაგები Source Maps-ის **გარეშე** (`sourcemap: false`)

### 2.1 ბაგი #1 — ORD-1004

| | პასუხი |
|---|---|
| რა მოხდა ეკრანზე | |
| შეცდომის ტიპი და ტექსტი | |
| stack trace-ის პირველი ხაზი | |
| ფაილი : ხაზი : სვეტი | |
| ფუნქციის სახელი | |

**რა ვერ გაიგე ამ stack trace-იდან:**

### 2.2 ბაგი #2 — ORD-1005

| | პასუხი |
|---|---|
| შეცდომის ტექსტი | |
| stack trace-ის პირველი ხაზი | |
| რომელი კომპონენტი ბრალდება | |

### 2.3 ბაგი #4 — კუპონი

| | პასუხი |
|---|---|
| შეცდომის ტექსტი სრულად | |
| Minified React error # | |
| React-ის შემოთავაზებული ბმული | |

**Error decoder-ის სრული ტექსტი:**

**რატომ ჩაანაცვლა React შეტყობინება ნომრით:**

### 2.4 შუალედური დასკვნა (2-3 წინადადება)

---

## 3. Source Maps-ის ჩართვა (`sourcemap: true`)

### 3.1 ბილდის შედარება

| | `false` | `true` |
|---|---|---|
| ფაილების რაოდენობა `dist/assets/`-ში | | |
| JS ფაილის ზომა | | |
| `.map` ფაილის ზომა | — | |
| `dist/` ჯამური ზომა | | |
| ბოლო ხაზი JS ფაილში | | |

**გაიზარდა მომხმარებლის ჩამოტვირთული JS? რატომ (არა):**

### 3.2 იგივე ბაგები — წაკითხვადი

| ბაგი | ფაილი : ხაზი | ფუნქცია / კომპონენტი | მიზეზი |
|---|---|---|---|
| #1 (ORD-1004) | | | |
| #2 (ORD-1005) | | | |
| #4 (კუპონი) | | | |

**სრული stack trace ერთი ბაგისთვის:**

```
```

**Sources panel-ში ორიგინალი კოდი ჩანს:** დიახ / არა
**Breakpoint production ბილდზე იმუშავა:** დიახ / არა — ნანახი ცვლადი: `____________`

---

## 4. ბაგების გამოსწორება

### 4.1 ბაგი #1 — `Cannot read properties of undefined`

**აკლია ველი:** ____________ (`data.js`, ORD-1004)
**სად „სკდება" კოდი:** ____________

**რატომ არ ჩავარდა `calculateTotals` და ჩავარდა შემდეგი ფუნქცია:**

```js
// before

// after (მონაცემების დაცვა)

// after (formatMoney-ის მედეგობა)
```

### 4.2 ბაგი #2 — `Objects are not valid as a React child`

**რითი განსხვავდება ORD-1005:**

```jsx
// before

// after
```

### 4.3 ბაგი #3 — `key` prop

**გამაფრთხილებლის სრული ტექსტი:**

**რომელი კომპონენტი / `.map()`:**

**production-ში ჩანს?** დიახ / არა — **რატომ:**

**რა აირჩიეთ `key`-ად და რატომ:**

```jsx
// before

// after
```

### 4.4 ბაგი #4 — `Too many re-renders`

**სად იძახება `setState` render-ის ფაზაში:**

**უსასრულო ციკლი 3 ნაბიჯად:** `____ → ____ → ____`

**არჩეული გამოსწორება (event handler / useEffect) და რატომ:**

```jsx
// before

// after
```

### 4.5 ბაგი #5 — `Cannot update a component while rendering a different component`

**შეტყობინების სრული ტექსტი:**

**დასახელებული ორი კომპონენტი:** ____________ და ____________

**არჩეული გამოსწორება და რატომ:**

```jsx
// before

// after
```

### 4.6 შემაჯამებელი ცხრილი

| # | შეცდომის ტიპი | ფაილი : ხაზი | რომელ რეჟიმში ჩანს | გამოსწორება |
|---|---|---|---|---|
| 1 | | | | |
| 2 | | | | |
| 3 | | | | |
| 4 | | | | |
| 5 | | | | |

---

## 5. Source Map ფაილის სტრუქტურა

| ველი | მნიშვნელობა / აღწერა |
|---|---|
| `version` | |
| `file` | |
| `sources` (რაოდენობა) | |
| `sourcesContent` | |
| `names` (მაგალითები) | |
| `mappings` | |

**`src/`-ის ფაილები `sources`-ში:**

**ზომები:** `.js` = ____ kB | `.js.map` = ____ kB — **რომელია დიდი და რატომ:**

---

## 6. უსაფრთხოება

### 6.1 რა ჩანს .map ფაილში

| რას ვეძებთ | `.js`-ში | `.js.map`-ში |
|---|---|---|
| `api/internal/pricing-rules` | | |
| `admin token` | | |

**რომელი სტრინგია ორივეგან, რომელი მხოლოდ `.map`-ში და რატომ:**

**მთელი კომენტარი ჩანს .map-ში?** დიახ / არა

**4 რამ, რაც .map-იდან შეიძლება გაიგოს ნებისმიერმა:**

1.
2.
3.
4.

### 6.2 `'hidden'`

| | `true` | `'hidden'` |
|---|---|---|
| `.map` იქმნება? | | |
| `sourceMappingURL` კომენტარი | | |
| ბრაუზერი ტვირთავს? | | |
| DevTools-ში ორიგინალი კოდი | | |

**რა შეიცვალა ფაილის ბოლოში:**

**stack trace წაკითხვადია `'hidden'`-ზე?** დიახ / არა

### 6.3 რეკომენდაციები

| გარემო | `sourcemap` | რატომ |
|---|---|---|
| development | | |
| staging | | |
| production | | |

**როგორ მოხვდება .map Sentry-ში და რა ნაბიჯი უნდა დაემატოს deploy-ს:**

**ბრძანება .map ფაილების წასაშლელად deploy-მდე:**

```bash
```

---

## 7. ბონუსი — ხელით მიბმული source map

**„Add source map..." იმუშავა?** დიახ / არა
**რას ნიშნავს ეს Sentry-ს მოდელისთვის:**

| `sourcemap` | build-ის დრო | `dist/` ზომა |
|---|---|---|
| `false` | | |
| `true` | | |
| `'hidden'` | | |

---

## 8. ვერიფიკაცია

- [ ] ORD-1004 იხსნება (ფასდაკლება `0.00 ₾`)
- [ ] ORD-1005 იხსნება, მისამართი იკითხება
- [ ] `DESK20` არ ანგრევს აპლიკაციას, კუპონი მუშაობს
- [ ] dev Console სუფთაა (`key`, „Cannot update a component")
- [ ] ყველა 5 შეკვეთა იხსნება, ჯამები სწორია
- [ ] `npm run build` წარმატებით სრულდება
- [ ] `vite.config.js` → `sourcemap: 'hidden'`
- [ ] `npm run preview` — Console-ში წითელი შეცდომა არ არის

---

## 9. დასკვნები

1. **რა ვერ გაიგე production stack trace-იდან source maps-ის გარეშე?**

2. **რომელი ბაგი აღმოჩნდა ყველაზე რთული და რატომ?**

3. **რომელი ბაგი production-ში „უხილავია" და რა რისკია ეს?**

4. **რატომ არ უნდა აიტვირთოს `.map` ფაილი საჯარო სერვერზე? (2-3 წინადადება)**

5. **როგორ გამოიყურება „სწორი" production კონფიგურაცია და რატომ?**

---

## 10. Screenshot-ები

| # | აღწერა | ფაილის სახელი |
|---|---|---|
| 1 | Console — წაუკითხავი stack trace (`sourcemap: false`) | |
| 2 | Console — წაკითხვადი stack trace (`sourcemap: true`) | |
| 3 | „Minified React error #NNN" + error decoder | |
| 4 | Sources panel — ორიგინალი ფაილი breakpoint-ით | |
| 5 | dev vs prod Console — გამაფრთხილებლის განსხვავება | |
| 6 | ტერმინალი — `grep` .map ფაილში | |
| 7 | ბონუსი — „Add source map..." | |
